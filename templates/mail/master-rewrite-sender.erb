#Enter rewrite rules, for example:
#Rewrite all mail from domain to noreply@domain
#/^(.*@)(.*)$/     noreply@${2}
#Rewrite all mail from user to user@newdomain.com
#/^(.*@)(.*)$/     {1}@newdomain.com
